version: 2
models:
  - name: stg_hubspot__contacts
    columns:
      - name: contact_id
        tests: [not_null, unique]
  - name: stg_hubspot__companies
    columns:
      - name: company_id
        tests: [not_null, unique]
  - name: stg_hubspot__deals
    columns:
      - name: deal_id
        tests: [not_null, unique]
